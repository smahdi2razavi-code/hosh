/* ------------------------------------------------------------------
   تنظیمات بازی «سلطان معما»
   ------------------------------------------------------------------
   این فایل را «پنل مدیریت» می‌سازد (پوشه admin-panel پروژه را در
   مرورگر لپ‌تاپ باز کن). فایل ساخته‌شده را همین‌جا جای‌گزین کن و
   بازی را دوباره بساز.

   اگر این فایل خالی باشد، بازی با مقدارهای پیش‌فرض اجرا می‌شود.
   ------------------------------------------------------------------ */
window.SULTAN_CONFIG = { v: 1 };
